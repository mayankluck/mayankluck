<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.useradvent">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE"/>

    <application
        android:allowBackup="true"
        android:name=".Useradvent"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Useradvent">
        <activity android:name=".ui.News.DetailedNews" />
        <activity android:name=".ui.Profile.Profile" />
        <activity android:name=".ui.Election.OpenElectionUrl" />
        <activity android:name=".ui.PublicIssues.VideoPlayers" />
        <activity android:name=".ui.Data.VoterFullDetails" />
        <activity android:name=".FullImageView" />
        <activity android:name=".userNavigation">

        </activity>
        <activity android:name=".ui.Data.VoterDetails" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>